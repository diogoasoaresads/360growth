/** Incremental build identifier — bump when deploying a new release. */
export const BUILD_ID = "0001";

/** Date of the last release (ISO 8601, local timezone). */
export const UPDATED_AT = "2026-02-28";

/** Timezone used for UPDATED_AT. */
export const UPDATED_TZ = "America/Sao_Paulo";
